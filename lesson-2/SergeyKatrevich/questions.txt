-------------- Настройка ------------------
Как настроить tsconfig.json так чтобы область компиляции была персональная для каждого файла?

--------------  TSLint------------------
 - Вот для такой строчки
type menuItem = {title: string, mark?: string, link?: string, items?: menuList};
 ... tslint пишет "Use an interface instead of a type literal"
   Почему так? Что плохого в использовании литрала для одного конкретного объекта?
   
 - почему tslint ругается на вот такой цикл for-of
 for (let a of menu) { }
... и требует const? Это странно ведь a всё время связывается с новым значением ...
 

-------------- Typescript ООП ------------------
 - Имеет ли смысл все закрытые методы описывать как protected (кроме явной необходимости в private),
из соображений гибкости - чтобы в классах наследниках можно было ими оперировать?
 
 - Методы помечаются как protected только чтобы в методах класса наследника их можно было вызывать?
Или чтобы ещё можно было их переопределять? Или в классах наследниках можно переопределять любые методы?

-------------- Другое ------------------
 - Можно ли получать в runtime'e JS мета-информацию о типах?